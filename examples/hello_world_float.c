#include "preamble/lib_preamble.h"

// fun main() : int {
int main() {
	// var x : float = 10.2;
	float x = 10.2;
	// var y : int = 11;
	int y = 11;

	// lib.print((x / 2) + (y / 2));
	printf("%f", (x / 2) + (y / 2));
// }
	return 0;
}
