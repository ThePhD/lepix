fun fib(x:int): int
{
  if (x < 2) {return 1;}
  return fib(x-1) + fib(x-2);
}

fun main(): int
{
  lib.print(fib(0));
  lib.print(fib(1));
  lib.print(fib(2));
  lib.print(fib(3));
  lib.print(fib(4));
  lib.print(fib(5));
  return 0;
}
