fun main () : int {
	for (var x : int = 0 to 10) {
		var x : int = lib.random_int(0, 40);
		if (x < 20) {
			lib.print("It's less than 20!");
		}
		else {
			lib.print("It's equal to or greater than 20");
		}
	}
}